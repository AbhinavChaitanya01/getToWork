// In this react app we are using React Context APIs so as to manage the state of items present in separate components.
import { createContext } from "react";
const JobContext = createContext();
export default JobContext;